[project]
name = "mono-copilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.17.7",
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "apps/copilot",
    "packages/runtime",
]
